# Normalize line endings to LF for all text files
* text=auto eol=lf

# Ensure YAML files always use LF
*.yml text eol=lf
*.yaml text eol=lf

# Markdown files
*.md text eol=lf

# Groovy / code files
*.groovy text eol=lf
